import os
from twilio.rest import CLient

client = Client(account_sid, auth_token)
if (percentDifference > 2): 
  client.messages.create(
   to = os.environ["phone"],
   from = "+18587077533"
   percentDifference = 3
   body = "The percent difference in stock since " + %d + " is " + %d + "%" % (previousValue, percentDifference) #first %d is time previous second is percent difference
)
