twiliocode = prompt("Twilio Code");
url = prompt("URL on nasdaq.com");
interval = prompt("How Often to Check(minutes):")
threshold = prompt("How much should stock value change to notify you?(percentage)")
alert("You will now be notified if the stock value of " + url + " changes")
